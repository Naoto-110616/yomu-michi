# 設計：AIが書き、人が直す\n\nこのプロダクトの前提を書き直したもの。以前は「ユーザーがエッジを投稿するSNS」として設計していたが、\n**AIを主体、ユーザーを補助**に反転させる。\n\n---\n\n## なぜ反転させるのが正しいか\n\nルールベースの生成器を実装して分かったことがある。\n\n| 関係 | 自動生成できた数 | 手段 |\n|---|---:|---|\n| 別視点 | 1378 | 賞・著者・ランキングの構造から機械的に |\n| 所属 | 113 | 概念の定義から |\n| 前提 | 24 | **ほぼ人手** |\n| 反論 | 5 | **人手のみ** |\n\n途中で「同じリストの年代前後を前提とみなす」規則を書いたら\n`博士の愛した数式 → 村上海賊の娘` のような無意味な線が量産され、全部取り消した。\n\nだが、これは「**ルールでは**作れない」という話であって、「作れない」ではない。\n前提と反論は、本を読んだ人間の知識があれば書ける。**そして LLM はその知識を持っている。**\n\nつまり正しい役割分担はこうなる。\n\n- **構造から出るもの** → プログラムが作る（今のパイプライン）\n- **読解から出るもの** → AIが作る（未実装。ここが本丸）\n- **正しいかどうか** → 人が決める\n\nユーザーの仕事は「投稿する」ではなく「**却下する**」。\n1件10秒の投稿より、1件1秒の判定のほうが桁違いに軽く、しかも精度に直結する。\n\n---\n\n## 役割分担\n\n| | AI | ユーザー |\n|---|---|---|\n| 本を集める | ◎ 常時・自動 | — |\n| 関係を書く | ◎ 常時・自動 | △ 気が向いたら |\n| 概念を作る | ○ 提案する | ◎ **名前をつける・却下する** |\n| 正しさを決める | × | ◎ **1タップで** |\n| 何を読むか | ○ 提案する | ◎ 決める |\n| 読んだ記録 | — | ◎（ブクログ同期） |\n\nユーザーが持つのは**拒否権と命名権**の2つだけ。それ以外はAIがやる。\n\n---\n\n## 4つのループ\n\n### 1. 収穫ループ（AI単独・常時）\n賞・ランキング・新刊を取り込んでノードを増やす。**実装済み**（`pipeline/`）。\n人は一切関与しない。ここに人を巻き込んだ時点で設計が間違っている。\n\n### 2. 推論ループ（AI単独・常時）— 未実装\n既存のノードから前提・反論・概念所属を推論し、**未検証エッジ**として置く。\n\n```\n入力: 本A, 本B, それぞれの書誌・出典・既存の関係\n出力: { type, why, confidence, evidence }\n```\n\n重要なのは **confidence と evidence を必ず返させる**こと。\n理由が書けない関係は、そもそも張るべきではない。\n\n### 3. 検証ループ（ユーザー・1タップ）\n未検証エッジを本ずつ出して `合ってる / 違う / どちらとも言えない`。\n**これが唯一の必須ユーザー操作**であり、プロダクトの中心的な体験。\n\nすでに MVP のフィードに組み込んだ「AIが仮に張った線 — 合っていますか？」がこれにあたる。\n当時はコールドスタート対策の補助機能として置いたが、**これが主役だった**。\n\n### 4. 編集ループ（ユーザー・任意）\n概念を足す、名前を変える、所属を直す。ブクログで自分のカテゴリを作るのと同じ感覚。\n\n---\n\n## 概念（コンセプト）の設計\n\n概念は本より上位のノードで、地図の背骨になる。\n`叙述トリック` `異種間のバディ` `大陸の東西軸` のような、**本と本のあいだにある軸**。\n\n### なぜ概念が要るか\n前提を「この本を読め」ではなく「**この概念さえ知っていればいい**」に落とせる。\n実際『銃・病原菌・鉄』の前提は本1冊ではなく「大陸の東西軸」という図ひとつだった。\n\n### ユーザーが作るのは「軸」だけ\n\n```\nユーザー: 概念を作る（名前 + 一言）\n   ↓\nAI:      候補の本を20冊出す\n   ↓\nユーザー: 要らないものをチェックを外す\n   ↓\nAI:      以後、新しい本が入るたびに所属を自動判定して提案し続ける\n```\n\n**中身はAIが埋め続ける。** ユーザーは器を作って、たまに掃除するだけ。\nこれが「AI中心・ユーザーは補助」の一番具体的な形。\n\n### 実装状況\n18個の概念を第一級ノードとして投入済み（`pipeline/concepts.py`）。\nAtlas 上では最も大きく、紫に発光するノードとして描かれ、\n`ネットワークのサイズ` を「概念だけ」にすると18個の軸だけが残る。\nそこから概念を1つ押すと、所属する本がその場に展開する。\n\n---\n\n## エッジのライフサイクル\n\n```\n                ┌──────────┐\n   AI推論 ───→  │ proposed │  薄い点滅線で描く\n                └────┬─────┘\n                     │ ユーザーの判定\n        ┌────────────┼────────────┐\n        ▼            ▼            ▼\n   ┌─────────┐  ┌──────────┐  ┌──────────┐\n   │verified │  │ rejected │  │ disputed │\n   │ 実線    │  │ 消える   │  │ 赤で残る │\n   └─────────┘  └──────────┘  └──────────┘\n```\n\n- `rejected` は消えるが**却下ログには残す**。同じ提案を二度出さないため。\n- `disputed`（賛否が割れた関係）は消さずに残す。\n  **学術的に論争がある関係がそのまま可視化される**——これは副産物ではなく、狙って残す価値がある。\n  『銃・病原菌・鉄』への環境決定論批判のような関係は、まさにここに入る。\n\n---\n\n## 信頼の設計\n\nAIを主体にすると必ず「それ本当？」が問題になる。ごまかさずに正面から作る。\n\n1. **すべてのエッジに理由を持たせる** — 実装済み。詳細パネルに必ず出る\n2. **出典があるものと、AIの推論だけのものを見た目で区別する**\n3. **自信度の低い線は薄く描く** — 見た目が信頼度になる\n4. **却下率を公開する** — 「前提エッジの却下率 32%」のように種類ごとに出す。\n   これは自慢ではなく**ユーザーが自分で信用度を較正するための材料**\n\n4つめが一番大事。AI中心のプロダクトが失敗するのは、精度が低いときではなく、\n**精度が低いことを隠したとき**なので。\n\n---\n\n## UIの原則（ユーザーが補助であることの帰結）\n\n- **空の状態を作らない。** AIが常に先に埋めている。「まず投稿してください」は禁じ手\n- **主要画面の動詞は「作る」ではなく「見る」「直す」**\n- **通知は依頼ではなく報告。** 「◯◯してください」ではなく「AIはこう考えました」\n- **1タップで終わらない操作を主導線に置かない**\n\n---\n\n## データモデル（次フェーズ / Supabase）\n\n```sql\nbooks     (id, isbn, title, author, year, cat, source)\nconcepts  (id, label, description, created_by, is_ai, created_at)\nedges     (id, from_id, to_id, type, why, evidence,\n           source,      -- 'rule' | 'llm' | 'human' | 'award-list'\n           confidence,  -- 0..1\n           status,      -- 'proposed' | 'verified' | 'rejected' | 'disputed'\n           proposed_by, created_at)\nverdicts  (id, edge_id, user_id, vote, note, created_at)\nshelf     (user_id, book_id, star, read_at)\nagent_runs(id, agent, kind, started_at, finished_at, stats)  -- AIの作業ログを人が見られるように\n```\n\n`agent_runs` を最初から入れておくのが肝。**AIが何をしたかが常に見える状態**を、\n後付けではなく最初から作る。\n\n---\n\n## AIの実行形態\n\nCron（GitHub Actions か Cloudflare Workers Cron）で毎日回す。\n\n1. 新刊・賞の発表を取り込む（収穫ループ）\n2. 未検証エッジが閾値を切ったら推論を回して補充する（推論ループ）\n3. ユーザーの★5から、まだ辿っていない道を1本選んで通知する\n\n3つめが、以前フィードの答えとして設計した「未踏の縁」。\n**AI中心にしたことで、これが「通知」ではなく「AIの日報」になる。**\n\n---\n\n## 今回入った分 / まだ入っていない分\n\n**入った**\n- 概念が第一級ノードになった（AIが提案する軸の受け皿）\n- ネットワークのサイズ = 概念 > 読んだ本 > 紐づく本 の階層\n- 概念からのドリルダウン（サイズ設定を無視して所属本が現れる）\n- ライブの力学シミュレーション（触ると全体が追従する）\n\n**入っていない**\n- `proposed` / `verified` の区別と検証UI（今は全部 verified 扱い）\n- 概念のユーザー登録（ボタンだけ置いてある）\n- LLMによる推論ループ\n\n次に着手するのは **推論ループと検証UI**。\nそこが動いて初めて、この設計は設計ではなく仕組みになる。\n\n---\n\n## 概念の最終設計：ハッシュタグ + 投票（2026-08 追記）\n\n概念を「ハッシュタグのようなものにするか」を決めた。答えは **ハッシュタグの軽さ + 投票の重み**。\n\n| ハッシュタグから借りるもの | ハッシュタグと変えるもの |\n|---|---|\n| 誰でもその場で付けられる軽さ | ただの文字列ではなく実体（説明・公式/ユーザー区分を持つ） |\n| 同名が自動で合流する | **誰が付けたかを保持する** — 人数が構造になる |\n\n### 強さの可視化\n\n`哲学 > 夜と霧` を紐付けるユーザーが増えるほど:\n\n1. **エッジが太く・明るくなる**（対数スケール、`concept_link_strength` ビューの人数）\n2. **本のノードが育つ**（紐付け人数の合計で半径が増える）\n3. 概念パネルの本リストが人数順に並ぶ = **「哲学を理解するにはまずこの本」が視覚で分かる**\n\nそこから先は既存の前提エッジが引き継ぐ:\n概念 → 一番太い本 → その本の「前提」→ 入門書、という導線が矢印で辿れる。\n\n### 公式概念とユーザー概念\n\n- **公式・大枠**: 哲学・思想 / 歴史・文明 / SF … の11個（`cat:*`）。最初は孤立していて、\n  票が集まるほど枝が生える。「大きな入口が民主的に形成される」設計\n- **公式・具体**: 叙述トリック / 異種間のバディ … の18個（`c_*`、AI提案の初期値）\n- **ユーザー概念**: 本の詳細から2タップで作成（`u_*`）。同名は unique 制約で自動合流\n\n### 世界中の本を扱う（遅延実体化）\n\n- カタログは持たない。検索は **国立国会図書館サーチ**（無料・キー不要・数千万件）を\n  同一オリジンの Worker (`/api/ndl`) でプロキシ + エッジキャッシュ\n- 「棚に入れる」「概念に紐付ける」が起きた瞬間だけ `books` テーブルに実体化\n- コストはカタログ規模ではなく利用量に比例する。LLM 推論も同じ原則で絞る\n\n### すぐ読み始める\n\n本の詳細に無料リンク4種: カーリル（図書館在架 / ISBN 直リンク）・Amazon・楽天ブックス・ブックオフ。\nAPI キー不要の検索 URL 方式。カーリル API（無料キー）に上げると在架状況の埋め込み表示まで行ける。\n\n### テーブル（追加分）\n\n```sql\nbooks          (key pk, isbn, title, author, publisher, year, cat, created_by)  -- 実体化した本のみ\nconcepts       (key pk, label unique, description, official, created_by)\nconcept_links  (concept_key, book_key, user_id, pk(全列))   -- 1人1票\nconcept_link_strength  -- view: 人数集計\n```\n